LIBDIR = circus/target/release
STATLIB = $(LIBDIR)/libcircus.a
PKG_LIBS = -L$(LIBDIR) -lcircus -lresolv

all: clean

$(SHLIB): $(STATLIB)

$(STATLIB):
	cargo build --release --manifest-path=circus/Cargo.toml

clean:
	rm -Rf $(SHLIB) $(STATLIB) $(OBJECTS) circus/target
